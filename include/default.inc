#include <mokoi>
#include <graphics>
#include <entities>
#include <fixed>
#include <string>
#include <map>

#tryinclude <game_default>

forward public Init(...);
forward public UpdatePosition();
forward public Close();
